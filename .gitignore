# IMPORTANT: Ignore all ".env" files. I'm being lazy and dumping shit into these .env files that shouldn't be pushed to github
*.env

# Ignore .vscode settings
/.vscode

# Ignore datalog files for the RickRollCrawler, but ensure that the file itself is still there
/RickRollCrawler/datalogs/*
